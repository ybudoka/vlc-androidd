<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="kankan.wheel" android:versionCode="2" android:versionName="2.1">
    <!--
        This is a library project: the <application> element it used to declare
        only carried a label, which the manifest merger reports as a conflict
        with the label of every application including it.
    -->
    <uses-sdk android:minSdkVersion="5" android:targetSdkVersion="7"/>
</manifest>
